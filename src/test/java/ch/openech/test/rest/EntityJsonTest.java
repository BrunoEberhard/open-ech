package ch.openech.test.rest;

public class EntityJsonTest {

	/*
	@Test
	public void testStringProperty() {
		Person person = new Person();
		person.firstName = "Felix";
		String json = EntityJsonWriter.write(person);
		Person person2 = EntityJsonReader.read(Person.class, json);
		Assert.assertEquals(person.firstName, person2.firstName);
	}
	
	@Test
	public void testListProperty() {
		Person person = new Person();
		PlaceOfOrigin origin = new PlaceOfOrigin();
		origin.originName = "Amden";
		person.placeOfOrigin.add(origin);
		String json = EntityJsonWriter.write(person);
		Person person2 = EntityJsonReader.read(Person.class, json);
		Assert.assertEquals(1, person2.placeOfOrigin.size());
		Assert.assertEquals("Amden", person2.placeOfOrigin.get(0).originName);
	}
	*/
}
